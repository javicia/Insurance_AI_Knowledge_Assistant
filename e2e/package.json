{
  "name": "insurance-ai-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "FASE 26: system-level Playwright E2E suite. Deliberately a separate module from frontend/ - these tests exercise the whole deployed system through the WAF (the one real browser-facing edge), not the Angular app in isolation, and must not be able to import frontend source or share its build. See docs/testing/E2E_PLAYWRIGHT.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}

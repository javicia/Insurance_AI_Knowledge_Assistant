/**
 * Inbound ports (driving side): use-case interfaces invoked by inbound adapters such as REST
 * controllers or Kafka listeners. Populated as each bounded context's use cases are
 * implemented (see {@code PROJECT_DISCOVERY.md} delivery plan) — intentionally empty in
 * FASE 1, which only establishes the layering and its enforcement.
 */
package com.rag.springai.insuranceai.ports.inbound;
